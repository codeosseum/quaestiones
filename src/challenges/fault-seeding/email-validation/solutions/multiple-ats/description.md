Does not need a dot (`.`) in the address.

## Examples

~~~~JavaScript
// returns true
emailValidation('a@bcom');

// also true because '.' is unescaped in the RegExp
emailValidation('a@b,com');
~~~~
