Accepts multiple `@` characters. Actually, accepts any character after the second `@`.

## Examples

~~~~JavaScript
// returns true
emailValidation('a@b@b.com');

// returns true
emailValidation('a@b.com@&&#');
~~~~
