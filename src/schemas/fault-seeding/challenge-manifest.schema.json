{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "http://codeosseum.com/schemas/fault-seeding/challenge-manifest.schema.json",

    "title": "Fault Seeding Challenge Manifest",
    "description": "File containing metadata for Fault Seeding challenges.",

    "type": "object",

    "properties": {
        "title": {
            "description": "The title of the challenge.",
            "type": "string"
        },

        "description": {
            "description": "A short description of the challenge.",
            "type": "string"
        },

        "assets": {
            "description": "URIs of additional assets (mostly images) accompanying the challenge.",
            "type": "array",
            "items": {
                "type": "string"
            }
        },

        "difficulty": {
            "description": "The difficulty of the challenge. Must be greater than or equal to zero.",
            "type": "integer"
        }
    }
}
